/* Base styling */
body {
  margin: 0;
  font-family: Arial, sans-serif;
  color: #222;
  background: linear-gradient(270deg, #eef2f3, #ffffff, #f9f9d2, #ffd6e0);
  background-size: 800% 800%;
  animation: gradientShift 20s ease infinite;
}

/* Gradient animation */
@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* Header */
header {
  background: #111;
  color: white;
  padding: 15px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* Navigation */
nav {
  text-align: center;
  background: #333;
  padding: 10px;
}
nav a {
  color: white;
  margin: 10px;
  text-decoration: none;
  font-weight: bold;
}

/* Hero and content sections */
.hero, .content {
  padding: 30px;
  text-align: center;
}

/* Projects grid */
.projects {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
}
.project-card {
  background: white;
  margin: 15px;
  padding: 15px;
  width: 250px;
  border-radius: 12px;
  box-shadow: 0 0 10px rgba(0,0,0,0.1);
}
.project-card img {
  width: 100%;
  border-radius: 10px;
  cursor: pointer;
}

/* Footer */
footer {
  text-align: center;
  padding: 15px;
  background: #111;
  color: white;
}

/* Buttons */
button {
  padding: 8px 12px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}
.admin-btn {
  background: orange;
  color: black;
}

/* Dark mode */
.dark {
  background: #121212;
  color: white;
}
.dark header,
.dark nav,
.dark footer {
  background: black;
}
.dark .project-card {
  background: #1e1e1e;
}
.dark nav a {
  color: #ddd;
}
